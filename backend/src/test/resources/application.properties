# needed for integration test
spring.mongodb.embedded.version=3.0.0
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

app.marketstack.password=${MARKETSTACK_PASSWORD:my-secret}
app.jwt.secret=${APP_JWT_SECRET:my-secret}